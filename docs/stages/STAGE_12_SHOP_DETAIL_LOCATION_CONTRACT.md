# 阶段 12：商户详情与位置距离

状态：已实现（Demo 范围）
更新时间：2026-09-04

`GET /v1/shops/{shopId}` 返回启用商户基础资料；经纬度必须同时提供且位于有效范围，提供时服务端计算米制直线距离。客户端按可视区域分别加载商品、点评和关联动态，详情首屏不被聚合集合阻塞。

商户业务 ID 全端为字符串。定位拒绝或失败时仍可浏览基础资料，真机定位验收未确认。
