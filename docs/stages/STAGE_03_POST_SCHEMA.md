# 阶段 3：统一动态、动态媒体与动态点赞契约

状态：已实现（Demo 范围）
更新时间：2026-09-04

`post`、`post_media`、`post_like` 是唯一社区动态模型。普通动态由服务端绑定 `ROAM_DAILY`，探店动态必须关联允许探店的分区和启用商户；媒体顺序与媒体资产分别唯一，点赞关系按用户和动态唯一。

Demo 直接重建，不转换旧 Blog、旧点赞或旧图片数据。开发种子的聚合计数与关系事实一致。
