# 阶段 23：订单关单、Mock 支付与多份发券

```yaml
designVersion: 3
designStatus: 已冻结
implementationStatus: 已实现
dependsOn: 阶段 22 已实现
affectedEnds: 后端、消费者小程序、商户小程序
```

## 目标

完成 15 分钟自动关单、消费者主动 Mock 支付、支付幂等和按数量逐份发券。

## 设计复核（v2）

- 支付交易以订单为边界保存唯一幂等键和网关结果；重复成功请求只回查原交易，不重复改变订单、销量或券实例。
- Mock 网关支持成功、失败和禁用三种结果：失败记录 `FAILED`（支付失败）但订单仍保持 `PENDING_PAYMENT`（待支付），禁用或生产未配置真实渠道返回 503。
- 关单任务与支付请求均使用 `status = PENDING_PAYMENT` 条件更新取得唯一迁移权；返库只在关单迁移成功后执行一次。
- 发券按订单 `quantity`（购买数量）生成 `sequence_no`（序号）从 1 开始的实例，订单与券实例采用组合唯一约束保障重放安全。
- 订单详情通过 `serverTime`（服务端时间）计算倒计时，客户端归零只回查，不在本地写入订单状态。

## 设计复核（v3：商户订单查询权限收口）

- 商户订单查询固定为 `GET /v1/merchant/orders`，只返回当前激活账号所属门店的订单快照，状态筛选仍使用后端权威订单枚举。
- 服务端在读取门店数据前强制校验 `merchant:order:read`（商户订单查看）权限；`OWNER`（店主）和 `MANAGER`（店长）允许，`VERIFIER`（核销员）明确返回 403 `MERCHANT_FORBIDDEN`（商户权限不足）。
- 权限校验使用数据库中的角色、账号状态和门店激活事实，不能由小程序菜单隐藏替代；无权限请求不得触发订单查询或泄露门店存在性。
- 商户端只读展示订单，继续使用字符串业务 ID、中文状态和真实空列表；分页参数保持 1 至 100 的服务端约束。

## 进入条件与涉及端

- 进入条件为阶段 22 已实现，待支付订单、服务端计价、库存扣减和幂等下单已通过并发验收。
- 涉及后端、消费者小程序和商户小程序；商户端只增加订单只读查询，不处理支付命令。

## 状态机、数据与权限

- 订单从 `PENDING_PAYMENT`（待支付）进入 `PAID`（已支付）或 `CANCELED`（已取消）；支付从 `PENDING`（待支付）进入 `SUCCEEDED`（支付成功）、`FAILED`（支付失败）或 `CLOSED`（已关闭）。
- 新增 `payment_transaction`，按订单数量生成多行 `user_voucher`，并更新订单、库存和销量；字段、组合唯一约束与快照以数据库契约为准。
- 只有订单所属消费者可支付和查看逐份券；SnailJob 使用受控内部身份执行关单，不能绕过业务条件更新。

## 后端

- `paymentExpireTime` 与创建时间由同一 `Clock` 生成并固定相差 15 分钟；查询和支付执行惰性关单。
- SnailJob 分页扫描过期 `PENDING_PAYMENT`（待支付）订单；条件更新取得唯一关单权并只返还一次库存。
- 支付网关模式为 `MOCK`（模拟）、`DISABLED`（禁用），预留 `WECHAT`（微信支付）；prod 未配置真实支付返回 503。
- 支付成功原子写入 `payment_transaction`、更新订单、增加销量并按数量生成 `user_voucher`；`order_id,sequence_no` 唯一。
- 单券实付按整数分摊，无法整除的分差按顺序分配，合计严格等于订单实付。

## 接口

- `POST /v1/users/me/orders/{orderId}/payments` 要求 `Idempotency-Key`。
- 订单详情返回 `serverTime`、`paymentExpireTime`、支付状态、履约摘要和逐份券摘要。
- 过期支付返回 `ORDER_PAYMENT_EXPIRED`（订单支付已超时），重复成功请求返回相同事实。

## 消费者小程序

- 订单详情按服务端时间显示倒计时；归零只触发刷新，不在本地修改状态。
- dev/test 显示“模拟支付”；处理中禁止重复点击，成功刷新订单并进入券包。
- 订单列表和详情展示 `PAID`（已支付）或 `CANCELED`（已取消）等中文状态以及多份券摘要。
- 商户订单页消费 `GET /v1/merchant/orders`，仅对拥有 `merchant:order:read`（商户订单查看）权限的激活店主/店长开放；核销员访问显示权限错误。

## 失败处理

- 倒计时归零、任务关单和支付竞争时，只允许一个数据库状态迁移成功；失败方回查并返回权威结果。
- Mock 支付失败保留待支付或明确失败事实，不发券、不增销量；支付服务禁用返回 503。
- 任务可重复投递，任何重放都不得二次返库、二次记支付、二次增销量或重复发券。
- 权限拒绝在查询前完成，禁止通过修改状态筛选、分页或门店 ID 绕过门店隔离。

## 验收

- 可注入时钟覆盖 14:59、15:00、重复扫描、支付/关单竞态和只返库一次。
- Mock 成功、失败、延迟、重放、销量和逐份发券数据库测试通过。
- SnailJob 真实执行器、惰性关单、消费者倒计时和运行时 OpenAPI 通过。

## 实施记录

- 已完成支付交易表、Mock 支付接口、幂等结果回查和禁用渠道 503 响应。
- 已完成支付/关单条件更新竞态、定时扫描、库存一次性返还和按数量序号发券。
- 已完成消费者订单详情服务端倒计时、模拟支付按钮和券包摘要展示。
- 自动化验证：后端 `mvn -q test`、`mvn -q -DskipTests compile`；消费者与商户小程序 `npm run verify`、`npm run build:npm`；商户 `VERIFIER`（核销员）订单权限单测覆盖 403。
- 真机扫码与微信支付未接入，按契约记录为未确认/生产扩展，不影响 Demo 阶段状态。
