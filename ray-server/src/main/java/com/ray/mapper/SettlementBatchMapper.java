package com.ray.mapper;import com.baomidou.mybatisplus.core.mapper.BaseMapper;import com.ray.entity.SettlementBatch;import org.apache.ibatis.annotations.*;public interface SettlementBatchMapper extends BaseMapper<SettlementBatch>{@Select("SELECT * FROM settlement_batch WHERE shop_id=#{shopId} AND settlement_date=#{date} LIMIT 1")SettlementBatch findByShopDate(@Param("shopId")Long shopId,@Param("date")java.time.LocalDate date);}
