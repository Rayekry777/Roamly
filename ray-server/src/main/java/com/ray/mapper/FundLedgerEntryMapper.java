package com.ray.mapper;import com.baomidou.mybatisplus.core.mapper.BaseMapper;import com.ray.entity.FundLedgerEntry;import org.apache.ibatis.annotations.*;public interface FundLedgerEntryMapper extends BaseMapper<FundLedgerEntry>{@Select("SELECT * FROM fund_ledger_entry WHERE business_event_id=#{event} AND entry_type=#{type} AND account_side=#{side} LIMIT 1")FundLedgerEntry findUnique(@Param("event")String event,@Param("type")String type,@Param("side")String side);}
